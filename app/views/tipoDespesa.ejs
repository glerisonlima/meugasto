<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>MeuGasto</title>
    <link rel="stylesheet" href="libs/bootstrap/dist/css/bootstrap.min.css" media="screen" title="no title">
    <link rel="stylesheet" href="css/estilo.css" media="screen" title="no title">
  </head>
  <body>
    <!-- Barra de Navegaçao -->
    <header>


      <nav class="navbar navbar-default navbar-fixed-top">
        <div class="container-fluid">
          <div class="navbar-header">
            <button type="button" class="collapsed navbar-toggle" data-toggle="collapse"
            data-target="#bs-example-navbar-collapse-6" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            </button>
            <a href="#" class="navbar-brand">Meu Gasto</a>
          </div>
          <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-6">
            <ul class="nav navbar-nav dropdown">
              <li><a href="/">Home</a></li>
              <li>
                <a class="dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Receitas <span class="caret"></span></a>
                  <ul class="dropdown-menu">
                    <li><a href="/tipoReceita">Tipos de Receitas</a></li>
                    <li><a href="/novaReceita">Nova Receitas</a></li>
                  </ul>
              </li>
              <li>
                <a class="dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Despesas <span class="caret"></span></a>
                <ul class="dropdown-menu">
                  <li><a href="/tipoDespesa">Tipos de Despesas</a></li>
                  <li><a href="/novaDespesa">Nova Despesa</a></li>
                </ul>
              </li>
            </ul>
          </div>
        </div>
      </nav>
  </header><br><br><br>
    <!-- Fim Barra de Navegaçao -->

    <!-- Corpo da pagina -->
    <section>
      <article>
        <div class="container">
          <% if(validacao.length > 0) { %>
          <div class="row">
            <div class="col-md-12">
              <div class="alert alert-danger">
                <strong>Atenção!</strong>
                <ul>
                  <% for(var i=0; i < validacao.length; i++) { %>
                  <li><%= validacao[i].msg %></li>
                  <% } %>
                </ul>
              </div>
            </div>
          </div>
          <% } %>
            <form action="/salvarTipoReceita" method="post">
              <div class="form-group">
                <label for="tipoReceita">Tipo Receita</label>
                <input type="text" class="form-control" name="tipoReceita" id="tipoReceita" placeholder="Descrição do Tipo de Receita">
              </div>
              <button type="submit" class="btn btn-default">Cadastrar</button>
            </form>

            <label for="tipoReceita">Tipos Receitas</label>
            <table class="table table-bordered">
              <thead>
                <tr>
                  <th>#</th>
                  <th>Descrição</th>
                </tr>
              </thead>
              <tbody>
                <% for(var i=0; i < receitas.length; i++) { %>
                <tr>
                  <th scope="row"><%= i+1 %></th>
                  <td><%= receitas[i].tipoReceita%></td>
                </tr>
                <% } %>
              </tbody>
            </table>
        </div>
      </article>
    </section>
    <!-- fim corpo da pagina -->

    <!-- Rodape da pagina -->
    <footer>

    </footer>

    <!-- fim rodape da pagina -->
    <script src="libs/jquery/dist/jquery.min.js"></script>
    <script src="libs/bootstrap/dist/js/bootstrap.min.js"></script>
  </body>
</html>
